# Models

This directory is reserved for speech recognition and language model artifacts
used by the backend (e.g., VOSK or Whisper models).

Due to their large size, model files are downloaded locally during setup
and are not committed to version control.
